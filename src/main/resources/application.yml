spring:
  datasource:
    username: tasks-app
    password: tasks-app
    url: jdbc:postgresql://tasks-db:5432/db
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create
