spring:
  banner:
    location: classpath:banner.txt
  datasource:
    username: tasks-app
    password: tasks-app
    url: jdbc:postgresql://tasks-db:5432/db
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create
